package org.example.class11;

import java.util.Scanner;

public class Task5 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String babe = "boy";
        System.out.println("Please enter mom's first name");
        String mom = scanner.nextLine();
        System.out.println("Please enter dad's first name");
        String dad = scanner.nextLine();

        /*if (babe.equals(babe)) {
            String(firstHalf = dad.substring(0, mom.length() / 2));
            String(lastHalf = dad.substring(dad.length()/2, dad.length()));
            System.out.println(firstHalf+lastHalf);
        }else{
            String(firstHalf = mom.substring(0, mom.length() / 2));
            String(lastHalf = dad.substring(dad.length()/2, dad.length()));
            System.out.println(firstHalf+lastHalf);
        }*/

    }
        }


